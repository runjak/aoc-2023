use std::{collections::HashMap, error::Error};

type Game = HashMap<str, u32>;
type GameLog = Vec<(u32, Game)>;

fn parse_game(line:&str) -> Game {
  
}

pub fn main() -> Result<(), Box<dyn Error>> {
    println!("02-1:");
    println!("Work in progress.");

    Ok(())
}
